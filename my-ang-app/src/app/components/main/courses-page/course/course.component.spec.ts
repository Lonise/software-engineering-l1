import { CourseComponent } from './course.component';

describe('CourseComponent', () => {
	let component: CourseComponent = new CourseComponent();
	it('should create', () => {
		expect(component).toBeTruthy();
	});
});
